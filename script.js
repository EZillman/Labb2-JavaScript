
/*
Theme toggle
skapa en knapp som växlar tema när man trycker på den
en boolean för att hålla reda på vilket färgtema sidan är i

När man trycker på knappen, anropa en funktion som kollar 
 vilket tema det är just nu ändra klass på body-elementet beroende på det.


if (islightChange) {
    darkTheme(); // ta bort ljus klass
                 // lägg på mörk klass
} else {
    lighTheme(); // ta bort mörk klass
                 // lägg på ljus klass
}

Använd classList
*/